#include "main.h"
/**
 * _isdigit - this program checks for digits
 * @c: int to check for digits
 * Return: 1 if c is digit, 0 if c is not digit
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
