#include "main.h"
/**
 * _islower - checks is there are small letters
 * @c: characters to check
 * Return: 1 if c is lowercase, 0 if c is not lowercase
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
